<UserControl x:Class="Hudebni_Prehravac_OctaBeats.Views.NastaveniView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hudebni_Prehravac_OctaBeats.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="{Binding Source={StaticResource LokalizaceConv}, ConverterParameter=Language}" FontWeight="Bold" Margin="5"/>
            <ComboBox ItemsSource="{Binding DostupneJazyky}" 
                      SelectedItem="{Binding VybranyJazyk}" 
                      DisplayMemberPath="NativeName" Width="200"/>
        </StackPanel>
    </Grid>
</UserControl>

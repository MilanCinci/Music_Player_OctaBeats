<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="Hudebni_Prehravac_OctaBeats.Views.PlaylistEditorDialogView"
        Title="Úprava playlistu"
        Width="800"
        Height="500"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Knihovna -->
        <GroupBox Header="Knihovna" FontSize="18" FontWeight="Bold">
            <ListBox ItemsSource="{Binding KnihovnaSkladby}"
                     SelectedItem="{Binding VybranaKnihovnaSkladba}" av:ItemsSource="{av:SampleData ItemCount=5}" FontWeight="Normal">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Nazev}" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="{Binding Interpret}" Foreground="Gray" FontSize="11" FontWeight="Normal"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <!-- ➕ / ➖ -->
        <StackPanel Grid.Column="1"
                    VerticalAlignment="Center"
                    Margin="12">
            <Button Content="➕"
                    Command="{Binding PridatCommand}"
                    Width="60"
                    Margin="0,5" FontSize="16"/>
            <Button Content="➖"
                    Command="{Binding OdebratCommand}"
                    Width="60"
                    Margin="0,5" FontSize="16"/>
        </StackPanel>

        <!-- Playlist -->
        <GroupBox Grid.Column="2"
                  Header="Playlist" FontSize="18" FontWeight="Bold">
            <ListBox ItemsSource="{Binding PlaylistSkladby}"
                     SelectedItem="{Binding VybranaPlaylistSkladba}"
                     av:ItemsSource="{av:SampleData ItemCount=5}" FontWeight="Normal">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Nazev}" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="{Binding Interpret}" Foreground="Gray" FontSize="11"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

        <!-- Název playlistu -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Název playlistu: " HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14"/>
        <TextBox x:Name="tboxPlaylist" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="40,0,40,0" FontSize="14"
                 Text="{Binding NazevPlaylistu, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

        <!-- OK button -->
        <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.Row="2" HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="100"
                    Margin="5,8,30,8"
                    Command="{Binding PotvrditCommand}" FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>
